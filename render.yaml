services:
  - type: web
    name: vm-api
    env: docker
    dockerfilePath: ./Dockerfile.api
    region: oregon
    plan: starter
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: HF_TOKEN
        sync: false